import Foundation

enum L10n {
    static let locationErrorTitle = "Locations permission needed to continue."
    static let locationErrorMessage = "Please provide \"Always allow\" permission from settings."
    static let openSettings = "Settings"
    static let cancel = "Cancel"
}
