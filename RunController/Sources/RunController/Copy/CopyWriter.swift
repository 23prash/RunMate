import Foundation

enum CopyWriter {
    static let duration = "Time (hh:mm:ss)"
    static let distanceKm = "Distance (km)"
    static let distanceMi = "Distance (mi)"
    static let paceKm = "Pace (min/km)"
    static let paceMi = "Pace (mi/km)"
}
